/* 
There are two main data types (Primitive & non-Primitive)

Primitive is further divided into:
    -String, Boolean, Number, null, undefined, symbol

non-Primitive is further divided into:
    -objects (which is subdivided into)
        -arrays, function, objects
*/